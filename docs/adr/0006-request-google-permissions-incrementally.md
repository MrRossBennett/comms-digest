# Request Google permissions incrementally

Comms Digest will separate Google identity, Gmail access, and Calendar access into three explicit authorization moments. The Household Owner first signs in, then grants read-only Gmail access during communication onboarding, and grants Calendar access only when enabling calendar features; each connection can be revoked without disabling the others.
