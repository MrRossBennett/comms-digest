# Use Braintrust for evaluation and development observability

Comms Digest will use Braintrust to run and compare TypeScript evaluations and to inspect development traces from LangGraph and Vercel AI SDK model calls. Braintrust will receive synthetic scenarios and development data only at first; real family communications remain outside the platform, with production recording only redacted operational metadata unless that boundary is deliberately revisited.
