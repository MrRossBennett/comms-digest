# Deploy the initial system on Railway

Comms Digest will initially deploy its web application, PostgreSQL database, scheduled ingestion, and background processing within one Railway project. This accepts Railway's small monthly cost in exchange for keeping long-running agent work, cron polling, persistence, and the application in one operational environment rather than splitting the system across serverless and worker providers.
