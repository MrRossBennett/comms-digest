# Select models through stage-level evals

Comms Digest will keep OpenAI and Anthropic model providers swappable and select models independently for each agent stage using measured accuracy, latency, and cost. Model choice is therefore configuration backed by eval results rather than a system-wide provider commitment, with every run recording the model and prompt versions needed to reproduce those results.
